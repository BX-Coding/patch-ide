# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
"on":
  push:
    branches:
      - stable
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build
        run: npm ci && npm run build
        env:
          ENVIRONMENT: "production"
          FIREBASE_WEB_API_KEY: "${{ secrets.FIREBASE_PROD_WEB_API_KEY }}"
          FIREBASE_AUTH_DOMAIN: "${{ vars.FIREBASE_PROD_AUTH_DOMAIN }}"
          FIREBASE_PROJECT_ID: "${{ vars.FIREBASE_PROD_PROJECT_ID }}"
          FIREBASE_STORAGE_BUCKET: "${{ vars.FIREBASE_PROD_STORAGE_BUCKET }}"
          FIREBASE_MESSAGING_SENDER_ID: "${{ vars.FIREBASE_PROD_MESSAGING_SENDER_ID }}"
          FIREBASE_APP_ID: "${{ secrets.FIREBASE_PROD_APP_ID }}"
          FIREBASE_MEASUREMENT_ID: "${{ secrets.FIREBASE_PROD_MEASUREMENT_ID }}"
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_PATCH_271D1 }}"
          channelId: live
          projectId: "${{ vars.FIREBASE_PROD_PROJECT_ID }}"
